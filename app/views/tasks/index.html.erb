<h1>タスク一覧</h1>

<ul>
    <% @tasks.each do |task|%>
  <li>No:<%= link_to task.id, task %> : <%= task.content %> &gt; <%= task.status %></li>
    <% end %>
</ul>
<%= link_to 'タスクの新規登録' , new_task_path %>